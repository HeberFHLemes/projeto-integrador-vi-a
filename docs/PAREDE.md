# Métodos de construção da parede de blocos
> Explicar como será definido, para cada nível, onde há blocos e onde há espaços, e então como isto será usado;
>
> Um exemplo, dado pelo próprio professor, é a construção de matrizes contendo valores booleanos ou binários (0/1),
> que então é passada para o responsável pela construção e posterior renderização de cada bloco,
> gerando um bloco "onde" há 1, e não "onde" há 0.
