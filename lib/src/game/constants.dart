// Constantes relativas ao jogo.
// Valores baseados nos definidos no codelab usado como apoio para o projeto.
// Demais informações (tamanho dos blocos, número de blocos por linha, etc.,
// serão definidas em outro componente da aplicação.
const gameWidth = 820.0;
const gameHeight = 1600.0;

const ballRadius = gameWidth * 0.02;

const paddleWidth = gameWidth * 0.2;
const paddleHeight = ballRadius * 2;
const paddleStep = gameWidth * 0.05;

const difficultyModifier = 1.03;